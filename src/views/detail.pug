extends layout.pug

block content 
  h1=music.title
  h3 가수: #{music.singer}
  h3 앨범명: #{music.albumTitle}
  img(src=`${music.coverImg}`)
  a(href="") Edit Music &rarr; 
  br
  a(href="") Delete Music &rarr;